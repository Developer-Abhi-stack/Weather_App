import Weather from "./components/Weather"; // Import the Weather component

const App = () => {
  return (
    <div className="app">
      {/* Render the Weather component inside the App component */}
      <Weather />
    </div>
  );
};

export default App; // Export the App component as default
